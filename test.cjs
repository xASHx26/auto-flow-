const quoted = (s) => JSON.stringify(s); const fs = require('fs'); const code = fs.readFileSync('src/App.jsx', 'utf-8'); const buildLocMatch = code.match(/function buildPlaywrightLocator[\s\S]*?\n\}/m); eval(buildLocMatch[0]); const a = {command: 'click', text: 'Music', elementType: 'checkbox', target: 'css=#hobbies-checkbox-3', isAssertion: true}; console.log('LOC:', buildPlaywrightLocator(a, 'py'));
